```javascript
// app/page.js

export default function Home() {
  return (
    <div>
      <h1>Welcome to my Next.js app!</h1>
      <p>This is a test.</p>
    </div>
  );
}
```

This corrected code places the page component inside the `app` directory, resolving the warning.